import React from "react";

const Header: React.FC = () => {
  return (
    <header className="h-header bg-gradient-to-r from-gray-800 to-gray-700 text-white flex items-center px-4 shadow-md">
      <h1 className="text-base font-semibold tracking-wide">АСУ ЦБ</h1>
    </header>
  );
};

export default Header;
